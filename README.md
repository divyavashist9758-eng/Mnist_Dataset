# Mnist_Dataset
This project trains a neural network on the MNIST dataset of 28×28 grayscale handwritten digits (0–9). After normalizing and reshaping the images, a CNN is built and trained. The model is evaluated on test data and achieves high accuracy in digit classification.
